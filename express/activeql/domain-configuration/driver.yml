entity:
  Driver:
    attributes:
      firstname: String!
      lastname:
        type: String!
        unique: firstname
      birthdate: Date
      licenceValid: Date!
    assocToMany: Car
    seeds:
      30:
        firstname:
          faker: name.lastName
        lastname:
          faker: name.firstName
        birthdate:
          eval: faker.date.past(50, new Date('2001-01-01') )
        licenceValid:
          eval: faker.date.future(20)
        Car:
          sample: Car
